public class PyTheorum {
    public static void main(String[] args) {
        /*
        int max = 23;
        int min = 5;
        int pow = 2;

        int num1 = (int)Math.round(Math.random() * (max - min) + min);
        int num2 = (int)Math.round(Math.random() * (max - min) + min);
        int num3 = (int)Math.round(Math.random() * (max - min) + min);
        int num4 = (int)Math.round(Math.random() * (max - min) + min);

        double Hypo;
        double Hypo1;

        Hypo=Math.sqrt(Math.pow(num1, 2) + Math.pow(num2, 2));
        System.out.println("Triangle one" + "\t\t" + "Side one: " + num1 + "\t\t" + " Side two: " + num2 + "\t\t" + " Hypotenuse: " + Hypo);

        Hypo1 = Math.sqrt(Math.pow(num3, 2) + Math.pow(num4, 2));
        System.out.println("Triangle one" + "\t\t" + "Side one: " + num2 + "\t\t" + " Side two: " + num4 + "\t\t" + " Hypotenuse: " + Hypo1);

        String a = "Mathematics";
        String b = "Computer Science";
        String c = (a + " and " + b);
        int n = c.length();
        int m = a.indexOf( "the" );
        int p = a.indexOf( "math" );
        String d = b.substring( 3, 5 );
        String e = b.substring( b.indexOf( "S" ) );
        System.out.println(n);
        System.out.println(m);
        System.out.println(p);
        System.out.println(d);
        System.out.println(e);

         */
        String test = (int)3.14 + "";
        System.out.println(test);
    }
}
